name: my-k8s-cluster
runtime:
  name: python
  options:
    virtualenv: venv
description: A Python program to deploy a Kubernetes cluster on AWS
config:
  pulumi:tags:
    value:
      pulumi:template: kubernetes-aws-python
  aws:profile:
    value: test-luislongo-nvbbandsre
